[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "case2audio"
version = "0.1.0"
description = "Turn layout-heavy PDFs into narration text and Amazon Polly audio."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "George Kolasa" }]
dependencies = [
  # Pin Docling's minor line so a model/API change cannot silently break extraction.
  "docling>=2.124,<2.125",
  "boto3>=1.43,<2",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<10",
  "ruff>=0.12,<1",
]

[project.scripts]
case2audio = "case2audio.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
